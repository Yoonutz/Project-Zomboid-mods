# No credentials are currently required by this repo.
# GitHub auth is handled by the `gh` CLI's own login, not by env vars here.
# This file exists as a placeholder in case a future mod needs one
# (e.g. a Steam Workshop upload token) -- copy to .env.local and fill in
# if/when that's actually needed. .env.local is git-ignored.
